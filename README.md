This requires following modules:

- sass
- server
- watch


Instructions:

- Use 'npm install' to install all packages
- Use command 'gulp' to start watching
- Use command 'gulp connect' to start live server

This requires node, gulp